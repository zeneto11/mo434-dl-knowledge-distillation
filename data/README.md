# Data

Dataset files are downloaded here by torchvision when `download: true` is used by the loaders.

Expected datasets:

- `FGVCAircraft`
- `Food101`

The training scripts accept `train_limit`, `val_limit`, and `test_limit` in the YAML configs for smoke runs.
